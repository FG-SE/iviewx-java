package calibration;

// Currently only supports the automatic calibration
// Manual calibration may be added in the future
public interface ETCalibrationManager {
	
	public void calibrate();
	
}
