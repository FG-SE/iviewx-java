// Plugins
apply plugin: 'java'

// Repositories
repositories {
    jcenter()
}

// Dependencies
dependencies {
	// Compile
    compile files('libs/IViewXAPI.jar')
    

    // Test
    testCompile 'junit:junit:4.12'
    testCompile 'org.mockito:mockito-core:1.+'
}
